# F1 Editor i18n 快速参考

## 🚀 快速开始（3步完成）

### **1. 在HTML中引入脚本**

```html
<script src="js/i18n.js"></script>
<script src="js/language-switcher.js"></script>
```

### **2. 初始化**

```javascript
document.addEventListener('DOMContentLoaded', async () => {
    await window.i18n.init();
    window.LanguageSwitcher.init();
    window.i18n.updatePageContent();
});
```

### **3. 添加data-i18n属性**

```html
<!-- 文本翻译 -->
<button data-i18n="common.save">Save</button>

<!-- Placeholder翻译 -->
<input data-i18n-placeholder="common.search" placeholder="Search...">

<!-- Title翻译 -->
<a data-i18n-title="common.settings" title="Settings">
```

---

## 📖 常用API

```javascript
// 获取翻译
window.i18n.t('common.save')

// 切换语言
window.i18n.setLanguage('zh-CN')
window.i18n.setLanguage('en')

// 获取当前语言
window.i18n.getLanguage()

// 更新页面
window.i18n.updatePageContent()

// 监听语言切换
window.addEventListener('languageChanged', (e) => {
    console.log(e.detail.lang);
});
```

---

## 🔑 翻译键速查

### 通用
- `common.save` - 保存
- `common.load` - 加载
- `common.cancel` - 取消
- `common.confirm` - 确认
- `common.apply` - 应用
- `common.search` - 搜索
- `common.settings` - 设置

### 编辑器
- `editor.title` - F1 Manager 数据库编辑器
- `editor.loadSave` - 加载存档
- `editor.saveFile` - 保存文件

### 标签页
- `tabs.drivers` - 车手
- `tabs.staff` - 工作人员
- `tabs.teams` - 车队
- `tabs.calendar` - 赛历
- `tabs.performance` - 性能
- `tabs.seasonMods` - 赛季模组

### 车手
- `driver.name` - 姓名
- `driver.team` - 车队
- `driver.overall` - 总评
- `driver.pace` - 节奏
- `driver.experience` - 经验

### 消息
- `messages.loading` - 加载中...
- `messages.success` - 操作成功
- `messages.error` - 操作失败
- `messages.saveSuccess` - 保存成功

---

## 🌐 翻译文件位置

```
src/locales/
├── zh-CN/translation.json  (中文)
└── en/translation.json     (英文)
```

---

## ⚡ 快捷方式

### 批量替换示例

```javascript
// 一次性更新所有标签页
document.querySelectorAll('.nav-link').forEach(link => {
    const text = link.textContent.toLowerCase().replace(/\s+/g, '');
    link.setAttribute('data-i18n', `tabs.${text}`);
});
```

### 手动添加翻译

```json
{
  "custom": {
    "myKey": "我的翻译"
  }
}
```

```html
<span data-i18n="custom.myKey">我的翻译</span>
```

---

## 🐛 常见问题

### 翻译不生效？
1. 检查data-i18n属性是否正确
2. 确认翻译文件中键名匹配
3. 查看浏览器控制台是否有警告

### 语言切换器不显示？
1. 确认已调用 `LanguageSwitcher.init()`
2. 检查Bootstrap 5是否正确加载

### JSON格式错误？
1. 使用JSON验证工具检查
2. 确保没有尾逗号
3. 字符串必须用双引号

---

## 📝 完整翻译清单

需要汉化的主要文件：
- [ ] `src/index.html` - 主界面
- [ ] `src/js/*.js` - JS中的消息提示
- [ ] 所有表单标签
- [ ] 所有按钮文本
- [ ] 所有标题和说明

---

**详细文档请查看：i18n集成指南.md**
