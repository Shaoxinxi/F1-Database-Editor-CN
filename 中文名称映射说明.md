# F1 Database Editor 中文名称映射系统

## 📋 概述

本系统实现了F1 Database Editor的车队名称、车手姓名等内容的中文显示，使用F1官方中文译名。

---

## 🎯 功能特性

### ✅ 已实现的功能

1. **车队名称中文化**
   - Ferrari → 法拉利车队
   - Red Bull → 红牛车队
   - Mercedes → 梅赛德斯车队
   - 等10支F1车队

2. **车手姓名中文化**
   - Verstappen → 维斯塔潘
   - Hamilton → 汉密尔顿
   - Zhou → 周冠宇
   - Tsunoda → 角田裕毅
   - 等25名车手

3. **工作人员职位中文化**
   - Technical Chief → 技术总监
   - Race Engineer → 比赛工程师
   - 等8个职位

4. **赛道名称中文化**
   - Bahrain → 巴林国际赛道
   - Shanghai → 上海国际赛车场
   - 等24条赛道

5. **比赛阶段中文化**
   - Qualifying → 排位赛
   - Race → 正赛
   - 等7个阶段

---

## 📁 文件结构

```
src/
├── data/
│   └── zh_CN_names.json          # 中文名称映射数据
├── js/
│   ├── nameMapper.js             # 名称映射管理器
│   └── frontend/
│       ├── stats.js              # 已集成：车手名称转换
│       ├── transfers.js          # 已集成：车手姓名格式化
│       └── seasonViewer.js       # 已集成：车队名称转换
└── index.js                      # 已导入nameMapper
```

---

## 🔧 技术实现

### 1. 名称映射管理器 (nameMapper.js)

**核心功能：**
- 异步加载JSON映射文件
- 提供多种匹配策略（精确、忽略大小写、部分匹配）
- 内置备用映射（fallback）
- 支持5种类型转换：车队、车手、工作人员、赛道、比赛阶段

**主要函数：**
```javascript
// 加载映射文件
loadChineseNames()

// 获取中文名称
getTeamName(englishName)      // 车队
getDriverName(englishName)    // 车手
getStaffTitle(englishTitle)   // 工作人员
getTrackName(englishName)     // 赛道
getSessionName(englishName)   // 比赛阶段

// 通用转换
getChineseName(name, type)

// 批量转换
translateNamesInText(text, names, type)
```

### 2. 集成点

#### stats.js - 车手统计页面
```javascript
// 在getName函数中转换车手姓名
export function getName(html) {
    let name = "..."; // 从HTML提取
    name = getDriverName(name); // 转换为中文
    return name;
}
```

#### transfers.js - 转会系统
```javascript
// 在format_name函数中转换姓氏
export function format_name(fullName, ...) {
    const lastName = "..."; // 提取姓氏
    const chineseLastName = getDriverName(lastName); // 转换
    spanLastName.textContent = chineseLastName;
}
```

#### seasonViewer.js - 赛季查看
```javascript
// 在formatTeamNameForDisplay中转换车队名
function formatTeamNameForDisplay(teamName, ...) {
    const chineseName = getTeamName(teamName); // 转换
    return chineseName;
}
```

---

## 📊 映射数据

### 车队名称 (15个)
```json
{
  "ferrari": "法拉利车队",
  "red_bull": "红牛车队",
  "mercedes": "梅赛德斯车队",
  "mclaren": "迈凯伦车队",
  "williams": "威廉姆斯车队",
  "alpine": "阿尔派车队",
  "aston_martin": "阿斯顿·马丁车队",
  "haas": "哈斯车队",
  "sauber": "索伯车队",
  "rb": "RB车队"
}
```

### 车手姓名 (25个)
```json
{
  "verstappen": "维斯塔潘",
  "hamilton": "汉密尔顿",
  "leclerc": "勒克莱尔",
  "norris": "诺里斯",
  "zhou": "周冠宇",
  "tsunoda": "角田裕毅"
}
```

---

## 🎨 显示效果

### 修改前
```
车手积分榜：
1. Verstappen - 450分
2. Hamilton - 380分
3. Leclerc - 320分

车队积分榜：
1. Red Bull - 650分
2. Ferrari - 580分
3. Mercedes - 520分
```

### 修改后
```
车手积分榜：
1. 维斯塔潘 - 450分
2. 汉密尔顿 - 380分
3. 勒克莱尔 - 320分

车队积分榜：
1. 红牛车队 - 650分
2. 法拉利车队 - 580分
3. 梅赛德斯车队 - 520分
```

---

## 🔍 匹配策略

系统使用4层匹配策略，确保最高准确率：

1. **精确匹配**：完全匹配键名
2. **忽略大小写**：Verstappen = verstappen
3. **部分匹配**：包含关系即可匹配
4. **备用映射**：使用内置fallback

---

## 🛠️ 维护和扩展

### 添加新车手
编辑 `src/data/zh_CN_names.json`：
```json
{
  "drivers": {
    "new_driver_surname": "中文姓名"
  }
}
```

### 添加新车队
```json
{
  "teams": {
    "new_team_name": "中文车队名"
  }
}
```

### 添加新赛道
```json
{
  "tracks": {
    "new_track_name": "中文赛道名"
  }
}
```

---

## ⚙️ 工作原理

```
用户加载页面
    ↓
自动加载 zh_CN_names.json
    ↓
存储到 chineseNames 对象
    ↓
显示名称时调用转换函数
    ↓
应用匹配策略
    ↓
返回中文名称（或原名）
```

---

## 📝 注意事项

1. **姓氏匹配**：车手姓名使用姓氏匹配（如"Verstappen"而不是"Max Verstappen"）
2. **大小写不敏感**：系统会自动处理大小写
3. **备用机制**：如果JSON加载失败，使用内置的fallback映射
4. **品牌名称**：部分品牌名称保留英文（如"RB车队"）

---

## 🚀 性能优化

- **一次性加载**：映射文件只加载一次
- **内存缓存**：加载后存储在内存中
- **快速匹配**：使用对象查找，时间复杂度O(1)
- **懒加载**：需要时才转换名称

---

## 📦 构建产物

构建后生成的文件：
- `dist/data/zh_CN_names.json` - 映射数据
- `dist/bundle.js` - 包含nameMapper逻辑
- `dist/index.html` - 主页面

---

## 🔄 版本更新

### 2024赛季阵容
当前映射基于F1 Manager 2024游戏数据。

### 2025赛季更新
需要在赛季结束后更新车手和车队映射。

---

## 📞 技术支持

如有问题或需要添加新名称，请：
1. 检查 `zh_CN_names.json` 是否包含该名称
2. 确认名称拼写是否正确
3. 查看浏览器控制台的加载日志

---

## ✅ 测试清单

- [x] 中文名称映射文件创建
- [x] nameMapper.js模块开发
- [x] stats.js集成
- [x] transfers.js集成
- [x] seasonViewer.js集成
- [x] 构建成功
- [ ] 浏览器测试车手名称
- [ ] 浏览器测试车队名称
- [ ] 浏览器测试转会系统
- [ ] 浏览器测试赛季查看器

---

**最后更新**: 2026年5月12日
**版本**: 1.0.0
**状态**: ✅ 已实现，待测试
