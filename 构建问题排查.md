# 构建问题排查指南

## 🔍 闪退问题排查

### 方法一：使用调试脚本（推荐）

**双击运行：**
```
build-debug.bat
```

这个脚本会：
- ✅ 逐步检查每个环节
- ✅ 显示详细错误信息
- ✅ 错误时自动暂停
- ✅ 生成错误日志

---

### 方法二：手动逐步排查

打开CMD（命令提示符），执行以下命令：

```bash
# 1. 进入项目目录
cd F:\ModFans\DatabaseEditor\F1-Database-Editor-CN

# 2. 检查Node.js
node -v

# 如果提示"不是内部或外部命令"，说明没安装Node.js
# 请访问 https://nodejs.org/ 下载安装
```

```bash
# 3. 检查npm
npm -v

# 如果正常显示版本号，继续下一步
```

```bash
# 4. 查看当前目录
dir

# 应该能看到 package.json 和 src 目录
```

```bash
# 5. 手动安装依赖
npm install

# 观察是否有错误信息
```

```bash
# 6. 手动构建
npm run build

# 观察是否有错误信息
```

---

## ❌ 常见错误及解决方案

### 错误1：未找到Node.js

**症状：**
```
'node' 不是内部或外部命令，也不是可运行的程序
```

**解决方案：**
1. 访问 https://nodejs.org/
2. 下载LTS版本（推荐18.x）
3. 安装时勾选"Add to PATH"
4. **重启电脑**或重启命令行
5. 重新运行 build.bat

---

### 错误2：npm install失败

**可能原因：**
- 网络问题
- 权限问题
- 磁盘空间不足

**解决方案：**

```bash
# 方案A：使用国内镜像
npm install --registry=https://registry.npmmirror.com

# 方案B：清除缓存后重试
npm cache clean --force
npm install

# 方案C：使用yarn
npm install -g yarn
yarn install
```

---

### 错误3：npm run build失败

**查看错误信息：**
```bash
# 运行构建并查看错误
npm run build

# 或查看错误日志
type build-error.log
```

**常见原因：**

#### 3.1 webpack配置错误

```bash
# 检查webpack.config.js是否存在
dir webpack.config.js

# 检查package.json中的build脚本
type package.json | findstr "build"
```

#### 3.2 依赖包缺失

```bash
# 重新安装依赖
rmdir /s /q node_modules
del package-lock.json
npm install
npm run build
```

#### 3.3 内存不足

```bash
# 增加Node.js内存限制
set NODE_OPTIONS=--max-old-space-size=4096
npm run build
```

---

### 错误4：路径包含中文或空格

**症状：**
构建时出现路径相关错误

**解决方案：**
确保项目路径不包含中文或空格：

```
✅ 正确: F:\ModFans\DatabaseEditor\F1-Database-Editor-CN
❌ 错误: F:\我的项目\F1 Database Editor
```

---

## 📋 完整排查清单

按顺序检查以下项目：

- [ ] Node.js已安装（运行 `node -v` 检查）
- [ ] npm可用（运行 `npm -v` 检查）
- [ ] 在项目目录中（运行 `dir` 查看文件）
- [ ] package.json存在
- [ ] src目录存在
- [ ] webpack.config.js存在
- [ ] 磁盘空间充足
- [ ] 路径无中文和空格
- [ ] 有足够的内存（建议4GB以上）

---

## 🔧 终极解决方案

如果以上方法都不行，尝试完整重建：

```bash
# 1. 进入项目目录
cd F:\ModFans\DatabaseEditor\F1-Database-Editor-CN

# 2. 删除旧文件
rmdir /s /q node_modules
del package-lock.json
del build-error.log

# 3. 重新安装
npm install --registry=https://registry.npmmirror.com

# 4. 重新构建
npm run build

# 5. 如果还失败，查看错误日志
type build-error.log
```

---

## 📞 获取帮助

### 1. 查看错误日志

```bash
# 运行调试脚本
build-debug.bat

# 或手动运行并保存日志
npm run build > build-log.txt 2>&1

# 查看日志
type build-log.txt
```

### 2. 提交问题反馈

提供以下信息：
- Node.js版本: `node -v`
- npm版本: `npm -v`
- 操作系统: Windows版本
- 错误日志: build-error.log的内容
- 项目路径: 完整路径

### 3. 联系支持

- GitHub Issues: https://github.com/Shaoxinxi/F1-Database-Editor-CN/issues
- ModFans社区: https://www.modfans.cc

---

## 💡 快速测试

运行这个命令快速测试环境是否正常：

```bash
cd F:\ModFans\DatabaseEditor\F1-Database-Editor-CN
node -v && npm -v && dir package.json && echo 环境正常！
```

如果显示"环境正常！"，说明基础环境没问题。

---

**建议：先运行 `build-debug.bat`，它会自动帮你定位问题！**
