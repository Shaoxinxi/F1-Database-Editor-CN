# 图片路径问题修复方案

## 🔍 问题原因

HTML中的图片使用相对路径 `../assets/images/xxx.png`，当部署到子目录 `/f1m-editor/` 时，路径解析错误。

**示例：**
```html
<!-- 当前路径（错误） -->
<img src="../assets/images/logos/ferrari.png">

<!-- 浏览器尝试访问 -->
https://www.modfans.xyz/assets/images/logos/ferrari.png  ❌

<!-- 应该访问 -->
https://www.modfans.xyz/f1m-editor/assets/images/logos/ferrari.png  ✅
```

---

## ✅ 解决方案（选择其一）

### **方案A：修改Nginx配置（推荐，无需修改代码）**

在宝塔面板中编辑Nginx配置：

```nginx
location /f1m-editor/ {
    alias /www/wwwroot/www.modfans.xyz/f1m-editor/;
    index index.html;
    try_files $uri $uri/ /f1m-editor/index.html;
    
    # 添加静态资源别名
    location /f1m-editor/assets/ {
        alias /www/wwwroot/www.modfans.xyz/f1m-editor/assets/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}
```

**步骤：**
1. 登录宝塔面板
2. 网站 → www.modfans.xyz → 设置
3. 配置文件
4. 找到或添加上面的配置
5. 保存并重载Nginx：`nginx -s reload`

---

### **方案B：修改webpack配置，使用绝对路径**

修改 `webpack.config.js`，添加publicPath：

```javascript
output: {
    path: path.resolve(__dirname, 'dist'),
    filename: 'bundle.js',
    publicPath: '/f1m-editor/'  // 添加这行
},
```

然后重新构建：
```cmd
npm run build
```

---

### **方案C：使用Base标签（最简单）**

在 `src/index.html` 的 `<head>` 中添加：

```html
<head>
  <meta charset="UTF-8">
  <base href="/f1m-editor/">
  <title>F1 Manager 数据库编辑器</title>
  <!-- ... -->
</head>
```

**优点：**
- ✅ 一行代码解决
- ✅ 所有相对路径自动修正
- ✅ 无需修改Nginx配置

**重新构建：**
```cmd
npm run build
```

---

## 🎯 推荐方案

**推荐使用方案C（base标签）**，原因：
1. 最简单，只需修改一行
2. 不影响其他配置
3. 自动处理所有相对路径（图片、CSS、JS）

---

## 📝 快速修复步骤（方案C）

### 1. 修改HTML

编辑 `src/index.html`，在 `<head>` 第二行添加：

```html
<head>
  <meta charset="UTF-8">
  <base href="/f1m-editor/">
  <title>F1 Manager 数据库编辑器</title>
```

### 2. 重新构建

```cmd
cd F:/ModFans/DatabaseEditor/F1-Database-Editor-CN
npm run build
```

### 3. 上传到服务器

将 `dist` 目录的所有文件上传到：
```
/www/wwwroot/www.modfans.xyz/f1m-editor/
```

### 4. 测试访问

访问：https://www.modfans.xyz/f1m-editor/

检查图片是否正常显示。

---

## 🔧 如果还是不行

### 检查文件权限

```bash
cd /www/wwwroot/www.modfans.xyz/f1m-editor
chmod -R 755 assets/
chown -R www:www assets/
```

### 检查Nginx日志

```bash
tail -f /www/wwwlogs/www.modfans.xyz.error.log
```

查看具体的404错误信息。

---

## ✅ 验证清单

- [ ] HTML中添加了 `<base href="/f1m-editor/">`
- [ ] 重新构建了项目
- [ ] 上传了dist的所有文件（包括assets目录）
- [ ] 服务器文件权限正确（755）
- [ ] 浏览器清空缓存后测试
- [ ] 检查浏览器开发者工具的网络面板

---

**选择方案C，我帮你修改HTML并重新构建！**
