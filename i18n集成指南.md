# F1 Database Editor - i18n国际化集成指南

## 📋 概述

本文档说明如何将i18n国际化系统集成到F1 Database Editor项目中，实现中英文切换功能。

---

## ✅ 已完成的工作

### 1. 文件结构

```
src/
├── locales/
│   ├── zh-CN/
│   │   └── translation.json      # 中文翻译文件
│   └── en/
│       └── translation.json      # 英文翻译文件
├── js/
│   ├── i18n.js                   # i18n核心配置
│   └── language-switcher.js      # 语言切换器组件
└── index.html                    # 主HTML文件
```

### 2. 翻译文件

已创建基础翻译键值对，包括：
- 通用按钮文本
- 标签页标题
- 车手表单字段
- 车队信息
- 赛季模组
- 消息提示

---

## 🔧 集成步骤

### **步骤1：在HTML中引入i18n脚本**

编辑 `src/index.html`，在 `<head>` 或 `<body>` 末尾添加：

```html
<!-- 在现有JS文件之前添加 -->
<script src="js/i18n.js"></script>
<script src="js/language-switcher.js"></script>
```

---

### **步骤2：初始化i18n**

在 `src/index.html` 的最底部（所有脚本加载完成后）添加：

```html
<script>
// 页面加载完成后初始化i18n
document.addEventListener('DOMContentLoaded', async () => {
    // 初始化i18n
    await window.i18n.init();
    
    // 创建语言切换器
    window.LanguageSwitcher.init();
    
    // 更新页面内容
    window.i18n.updatePageContent();
    
    console.log('[App] i18n初始化完成');
});
</script>
```

---

### **步骤3：在HTML元素中添加data-i18n属性**

#### **方式A：文本内容翻译**

```html
<!-- 修改前 -->
<button>Save</button>
<h1>F1 Manager Database Editor</h1>

<!-- 修改后 -->
<button data-i18n="common.save">Save</button>
<h1 data-i18n="editor.title">F1 Manager Database Editor</h1>
```

#### **方式B：Placeholder翻译**

```html
<!-- 修改前 -->
<input type="text" placeholder="Search...">

<!-- 修改后 -->
<input type="text" data-i18n-placeholder="common.search" placeholder="Search...">
```

#### **方式C：Title属性翻译**

```html
<!-- 修改前 -->
<a href="#" title="Settings">

<!-- 修改后 -->
<a href="#" data-i18n-title="common.settings" title="Settings">
```

---

### **步骤4：在JavaScript中使用翻译**

```javascript
// 在JS代码中使用
const message = window.i18n.t('messages.saveSuccess');
alert(message);

// 监听语言切换事件
window.addEventListener('languageChanged', (event) => {
    console.log('语言已切换为:', event.detail.lang);
    // 执行额外的更新逻辑
});
```

---

### **步骤5：添加语言切换按钮**（可选）

如果不想用固定的语言切换器，可以在任意位置添加：

```html
<!-- 方式1：下拉菜单 -->
<select onchange="window.i18n.setLanguage(this.value)">
    <option value="zh-CN">中文</option>
    <option value="en">English</option>
</select>

<!-- 方式2：按钮组 -->
<button onclick="window.i18n.setLanguage('zh-CN')">中文</button>
<button onclick="window.i18n.setLanguage('en')">English</button>
```

---

## 📝 翻译文件扩展

### 添加新的翻译键

编辑 `src/locales/zh-CN/translation.json` 和 `src/locales/en/translation.json`：

```json
{
  "driver": {
    "newField": "新字段",
    "anotherField": "另一个字段"
  }
}
```

然后在HTML中使用：

```html
<span data-i18n="driver.newField">新字段</span>
```

---

## 🌐 更新webpack配置

确保webpack能够复制locales目录到dist：

编辑 `webpack.config.js`，在 `plugins` 中添加：

```javascript
const CopyWebpackPlugin = require('copy-webpack-plugin');

module.exports = {
    // ... 其他配置
    plugins: [
        // ... 其他插件
        new CopyWebpackPlugin({
            patterns: [
                {
                    from: 'src/locales',
                    to: 'locales'
                }
            ]
        })
    ]
};
```

---

## 🚀 完整集成示例

### **index.html 示例片段：**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title data-i18n="editor.title">F1 Manager Database Editor</title>
    
    <!-- 现有CSS文件 -->
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <!-- 主标题 -->
    <h1 data-i18n="editor.title">F1 Manager Database Editor</h1>
    
    <!-- 标签页 -->
    <ul class="nav nav-tabs">
        <li class="nav-item">
            <a class="nav-link" data-i18n="tabs.drivers" href="#">Drivers</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" data-i18n="tabs.teams" href="#">Teams</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" data-i18n="tabs.calendar" href="#">Calendar</a>
        </li>
    </ul>
    
    <!-- 搜索框 -->
    <input type="text" class="form-control" 
           data-i18n-placeholder="common.search" 
           placeholder="Search...">
    
    <!-- 按钮 -->
    <button class="btn btn-primary" data-i18n="common.save">Save</button>
    <button class="btn btn-secondary" data-i18n="common.cancel">Cancel</button>
    
    <!-- 现有JS文件 -->
    <script src="bundle.js"></script>
    
    <!-- i18n相关文件 -->
    <script src="js/i18n.js"></script>
    <script src="js/language-switcher.js"></script>
    
    <!-- 初始化 -->
    <script>
    document.addEventListener('DOMContentLoaded', async () => {
        await window.i18n.init();
        window.LanguageSwitcher.init();
        window.i18n.updatePageContent();
    });
    </script>
</body>
</html>
```

---

## 🔍 调试技巧

### 1. 查看当前语言

```javascript
console.log(window.i18n.getLanguage());
```

### 2. 手动切换语言

```javascript
window.i18n.setLanguage('en');
```

### 3. 测试翻译

```javascript
console.log(window.i18n.t('common.save'));
```

### 4. 查看翻译文件是否加载成功

```javascript
console.log(window.i18n.translations);
```

---

## 📊 汉化优先级

建议按以下顺序逐步汉化：

### **第一阶段：核心界面（MVP）**
- [ ] 主标题和副标题
- [ ] 标签页导航
- [ ] 主要按钮（保存、加载、取消、应用）
- [ ] 搜索框

### **第二阶段：车手和车队**
- [ ] 车手表单字段
- [ ] 车队信息字段
- [ ] 数据表格标题

### **第三阶段：高级功能**
- [ ] 赛季模组界面
- [ ] 转会系统
- [ ] 规则设置
- [ ] 性能调整

### **第四阶段：完整汉化**
- [ ] 所有提示和消息
- [ ] 帮助文档
- [ ] 错误提示

---

## ⚠️ 注意事项

1. **翻译键命名规范**
   - 使用小驼峰命名：`common.save`
   - 使用点号分隔层级：`tabs.seasonMods`
   - 保持一致性

2. **性能优化**
   - 翻译文件会异步加载，需要等待`init()`完成
   - 避免在循环中频繁调用`t()`

3. **兼容性**
   - 确保所有浏览器支持`fetch` API
   - 如需兼容旧浏览器，添加polyfill

4. **维护**
   - 英文和中文翻译文件保持结构一致
   - 添加新翻译时，两个文件都要更新

---

## 🎯 下一步

1. **解决404问题**
   - 确认服务器文件路径正确
   - 检查Nginx配置

2. **开始汉化HTML**
   - 先汉化主要界面元素
   - 逐步扩展到所有页面

3. **添加2026赛季入口**
   - 在赛季模组标签页添加2026选项
   - 移除`d-none`类

4. **测试和部署**
   - 本地测试语言切换
   - 重新构建：`npm run build`
   - 上传到服务器

---

## 📞 获取帮助

如有问题，请查看：
- 浏览器控制台错误信息
- 翻译文件JSON格式是否正确
- 文件路径是否准确

---

**开始汉化吧！从最重要的界面元素开始，逐步完善中文支持。**
